# frozen_string_literal: true

RSpec.describe Openehr::Rest::Client do
  it "has a version number" do
    expect(Openehr::Rest::Client::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
